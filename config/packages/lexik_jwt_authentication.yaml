lexik_jwt_authentication:
    secret_key: '%env(resolve:JWT_SECRET_KEY)%'
    public_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    pass_phrase: '%env(JWT_PASSPHRASE)%'
    token_ttl: '%env(int:JWT_TOKEN_TTL)%' # in seconds, default is 3600
# For implement token blocklist functionality
# https://github.com/lexik/LexikJWTAuthenticationBundle/blob/3.x/Resources/doc/10-invalidate-token.rst
#    blocklist_token:
#        enabled: true
#        cache: cache.app
